export class User {
    constructor(public id:any, public username:string, public lastName:string,public firstName:string, public email:string, public address:string, public cellphone:string, public password:string, public userType:string ){

    }
}
